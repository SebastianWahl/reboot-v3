# Auth Re-Boot V3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter l'authentification Supabase (Google OAuth + magic link) avec historique des audits passés sur l'écran d'accueil.

**Architecture:** Installer `@supabase/supabase-js`, créer un client singleton et un hook `useAuth`, ajouter un `AuthScreen` devant l'app, lier les sessions sauvegardées au `user_id`, afficher l'historique dans `HomeScreen`.

**Tech Stack:** React 19, Vite, Supabase Auth (supabase-js v2), Tailwind CSS

---

## Task 0 : Configuration Supabase dashboard (étapes manuelles)

> Ces étapes se font dans le dashboard Supabase — pas de code.

**Files:** aucun fichier modifié

- [ ] **Étape 1 : Activer Google OAuth**

  Dans le dashboard Supabase → Authentication → Providers → Google :
  - Activer le provider
  - Créer un projet OAuth dans Google Cloud Console (console.cloud.google.com)
  - Ajouter Authorized redirect URI : `https://cfagrdqwmwnuspcuthjp.supabase.co/auth/v1/callback`
  - Copier Client ID et Client Secret dans Supabase
  - Dans "Redirect URLs" du dashboard Supabase, ajouter : `http://localhost:5174` et `https://reboot-v3.vercel.app`

- [ ] **Étape 2 : Vérifier Email (magic link)**

  Dans Authentication → Providers → Email :
  - S'assurer que "Enable Email provider" est activé
  - "Confirm email" peut rester désactivé pour simplifier le flow magic link

- [ ] **Étape 3 : Ajouter colonne user_id à reboot_sessions**

  Dans Table Editor → reboot_sessions → Add column :
  ```sql
  ALTER TABLE reboot_sessions
  ADD COLUMN user_id uuid REFERENCES auth.users(id);
  ```

- [ ] **Étape 4 : Activer RLS et créer les policies**

  Dans l'éditeur SQL du dashboard :
  ```sql
  -- Activer RLS
  ALTER TABLE reboot_sessions ENABLE ROW LEVEL SECURITY;

  -- Policy INSERT : un user ne peut insérer que ses propres sessions
  CREATE POLICY "Users can insert own sessions"
  ON reboot_sessions FOR INSERT
  WITH CHECK (auth.uid() = user_id);

  -- Policy SELECT : un user ne voit que ses propres sessions
  CREATE POLICY "Users can select own sessions"
  ON reboot_sessions FOR SELECT
  USING (auth.uid() = user_id);

  -- Policy UPDATE : un user ne peut mettre à jour que ses propres sessions
  CREATE POLICY "Users can update own sessions"
  ON reboot_sessions FOR UPDATE
  USING (auth.uid() = user_id);
  ```

- [ ] **Vérifier** : dans Table Editor → reboot_sessions, confirmer que la colonne `user_id` existe et que RLS est activé (cadenas fermé sur la table).

---

## Task 1 : Installer supabase-js et créer le client

**Files:**
- Modify: `package.json`
- Create: `src/lib/supabase.js`

- [ ] **Étape 1 : Installer la librairie**

  ```bash
  cd /Users/sebastianwahl/Desktop/Workspace_Claude/reboot-v3
  npm install @supabase/supabase-js
  ```

  Expected output : `added 1 package` (ou similaire), pas d'erreur.

- [ ] **Étape 2 : Créer le client singleton**

  Créer `src/lib/supabase.js` :

  ```js
  import { createClient } from '@supabase/supabase-js';

  const SUPABASE_URL = 'https://cfagrdqwmwnuspcuthjp.supabase.co';
  const SUPABASE_ANON_KEY = 'sb_publishable_-z2MEdTcZRyYN8FI726dkg_SCAybtbi';

  export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
  ```

- [ ] **Vérifier** : `npm run dev -- --port 5174` démarre sans erreur. Ouvrir la console — aucune erreur d'import.

- [ ] **Commit**

  ```bash
  git add src/lib/supabase.js package.json package-lock.json
  git commit -m "feat: install supabase-js and create client singleton"
  ```

---

## Task 2 : Créer le hook useAuth

**Files:**
- Create: `src/hooks/useAuth.js`

- [ ] **Étape 1 : Créer le hook**

  Créer `src/hooks/useAuth.js` :

  ```js
  import { useEffect, useState } from 'react';
  import { supabase } from '../lib/supabase';

  export function useAuth() {
    const [user, setUser] = useState(null);
    const [loading, setLoading] = useState(true);

    useEffect(() => {
      // Récupérer la session existante (gère aussi le callback OAuth depuis l'URL)
      supabase.auth.getSession().then(({ data: { session } }) => {
        setUser(session?.user ?? null);
        setLoading(false);
      });

      // Écouter les changements d'état auth
      const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
        setUser(session?.user ?? null);
        setLoading(false);
      });

      return () => subscription.unsubscribe();
    }, []);

    async function signInWithGoogle() {
      await supabase.auth.signInWithOAuth({
        provider: 'google',
        options: {
          redirectTo: window.location.origin,
        },
      });
    }

    async function signInWithEmail(email) {
      const { error } = await supabase.auth.signInWithOtp({
        email,
        options: {
          emailRedirectTo: window.location.origin,
        },
      });
      if (error) throw error;
    }

    async function signOut() {
      await supabase.auth.signOut();
    }

    return { user, loading, signInWithGoogle, signInWithEmail, signOut };
  }
  ```

- [ ] **Vérifier** : `npm run dev -- --port 5174` démarre sans erreur.

- [ ] **Commit**

  ```bash
  git add src/hooks/useAuth.js
  git commit -m "feat: add useAuth hook (Google OAuth + magic link + signOut)"
  ```

---

## Task 3 : Créer AuthScreen

**Files:**
- Create: `src/components/screens/AuthScreen.jsx`

- [ ] **Étape 1 : Créer le composant**

  Créer `src/components/screens/AuthScreen.jsx` :

  ```jsx
  import { useState } from 'react';
  import doctorClaude from '../../assets/doctor-claude.jpg';

  export default function AuthScreen({ onSignInWithGoogle, onSignInWithEmail }) {
    const [email, setEmail] = useState('');
    const [sent, setSent] = useState(false);
    const [loading, setLoading] = useState(false);
    const [error, setError] = useState(null);

    async function handleEmailSubmit(e) {
      e.preventDefault();
      if (!email.trim()) return;
      setLoading(true);
      setError(null);
      try {
        await onSignInWithEmail(email.trim());
        setSent(true);
      } catch (err) {
        setError('Erreur lors de l\'envoi. Vérifiez votre adresse email.');
      } finally {
        setLoading(false);
      }
    }

    return (
      <div className="min-h-screen bg-[#f5f0ea] flex flex-col items-center justify-center px-6">
        <div className="bg-white rounded-2xl shadow-lg p-8 w-full max-w-sm">

          {/* Logo */}
          <div className="flex items-center gap-3 mb-8">
            <div className="w-10 h-10 rounded-xl overflow-hidden flex-shrink-0">
              <img src={doctorClaude} alt="Doctor Claude" className="w-full h-full object-cover" style={{ transform: 'scale(1.6)', transformOrigin: 'center 55%' }} />
            </div>
            <div>
              <span className="font-semibold block" style={{ color: '#1a1209', fontFamily: "'EB Garamond', Georgia, serif", fontSize: '22px', lineHeight: 1.1 }}>Re-Boot</span>
              <span className="block" style={{ color: '#C96442', fontFamily: "'EB Garamond', Georgia, serif", fontStyle: 'italic', fontWeight: '600', fontSize: '13px' }}>with Doctor Claude</span>
            </div>
          </div>

          <h1 className="text-xl font-bold text-[#1a1209] mb-1">Bienvenue</h1>
          <p className="text-sm text-[#888] mb-6">Connectez-vous pour accéder à votre audit et consulter vos résultats.</p>

          {sent ? (
            <div className="bg-[#f0faf4] border border-[#a8d5b8] rounded-xl p-4 text-sm text-[#2d6a4f]">
              Lien envoyé à <strong>{email}</strong>. Vérifiez votre boîte mail.
            </div>
          ) : (
            <>
              {/* Google */}
              <button
                onClick={onSignInWithGoogle}
                className="w-full flex items-center justify-center gap-3 border border-[#e0ddd6] rounded-xl py-3 text-sm font-semibold text-[#1a1209] hover:bg-[#faf7f2] transition-colors mb-4"
              >
                <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
                  <path d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844a4.14 4.14 0 01-1.796 2.716v2.259h2.908c1.702-1.567 2.684-3.875 2.684-6.615z" fill="#4285F4"/>
                  <path d="M9 18c2.43 0 4.467-.806 5.956-2.18l-2.908-2.259c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 009 18z" fill="#34A853"/>
                  <path d="M3.964 10.71A5.41 5.41 0 013.682 9c0-.593.102-1.17.282-1.71V4.958H.957A8.996 8.996 0 000 9c0 1.452.348 2.827.957 4.042l3.007-2.332z" fill="#FBBC05"/>
                  <path d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 00.957 4.958L3.964 6.29C4.672 4.163 6.656 3.58 9 3.58z" fill="#EA4335"/>
                </svg>
                Continuer avec Google
              </button>

              {/* Séparateur */}
              <div className="flex items-center gap-3 mb-4">
                <div className="flex-1 h-px bg-[#e0ddd6]" />
                <span className="text-xs text-[#bbb]">ou</span>
                <div className="flex-1 h-px bg-[#e0ddd6]" />
              </div>

              {/* Magic link */}
              <form onSubmit={handleEmailSubmit} className="space-y-3">
                <input
                  type="email"
                  value={email}
                  onChange={e => setEmail(e.target.value)}
                  placeholder="votre@email.com"
                  required
                  className="w-full border border-[#e0ddd6] rounded-xl px-4 py-3 text-sm text-[#1a1209] placeholder-[#bbb] focus:outline-none focus:border-[#e07b39]"
                />
                {error && <p className="text-xs text-red-500">{error}</p>}
                <button
                  type="submit"
                  disabled={loading}
                  className="w-full rounded-xl py-3 text-sm font-semibold text-white transition-colors"
                  style={{ backgroundColor: loading ? '#ccc' : '#1a1209' }}
                >
                  {loading ? 'Envoi...' : 'Recevoir un lien de connexion'}
                </button>
              </form>
            </>
          )}
        </div>

        <p className="text-[10px] text-[#bbb] mt-6 text-center max-w-xs leading-relaxed">
          Disclaimer : Re-Boot with Doctor Claude est un audit propulsé par l'intelligence artificielle. À titre informatif uniquement.
        </p>
      </div>
    );
  }
  ```

- [ ] **Vérifier** : `npm run dev -- --port 5174`. Ouvrir `http://localhost:5174` — pour l'instant l'écran de connexion n'est pas encore affiché (App.jsx pas encore modifié).

- [ ] **Commit**

  ```bash
  git add src/components/screens/AuthScreen.jsx
  git commit -m "feat: add AuthScreen (Google + magic link)"
  ```

---

## Task 4 : Modifier App.jsx — gate auth + consulter session

**Files:**
- Modify: `src/App.jsx`

- [ ] **Étape 1 : Lire l'état actuel de App.jsx**

  Vérifier les imports en haut du fichier avant de modifier.

- [ ] **Étape 2 : Ajouter les imports**

  En haut de `src/App.jsx`, ajouter après les imports existants :

  ```js
  import { useState } from 'react';
  import { useAuth } from './hooks/useAuth';
  import AuthScreen from './components/screens/AuthScreen';
  ```

- [ ] **Étape 3 : Ajouter le hook et la gestion de la session passée à consulter**

  Dans la fonction `App()`, après la ligne `const savedSession = loadSession();`, ajouter :

  ```js
  const { user, loading: authLoading, signInWithGoogle, signInWithEmail, signOut } = useAuth();
  const [viewingSession, setViewingSession] = useState(null);
  ```

- [ ] **Étape 4 : Ajouter le gate auth avant le rendu principal**

  Juste après le bloc `if (new URLSearchParams(...) preview)`, ajouter :

  ```js
  if (authLoading) {
    return <LoadingScreen message="Chargement..." />;
  }

  if (!user) {
    return <AuthScreen onSignInWithGoogle={signInWithGoogle} onSignInWithEmail={signInWithEmail} />;
  }

  if (viewingSession) {
    return (
      <DiagnosticScreen
        registres={viewingSession.registres}
        diagnostic={viewingSession.diagnostic}
        onBack={() => setViewingSession(null)}
      />
    );
  }
  ```

- [ ] **Étape 5 : Passer user, signOut et setViewingSession à HomeScreen**

  Remplacer la ligne :
  ```js
  return (
    <>
      <HomeScreen onStart={handleStart} savedSession={savedSession} />
  ```
  Par :
  ```js
  return (
    <>
      <HomeScreen
        onStart={handleStart}
        savedSession={savedSession}
        user={user}
        onSignOut={signOut}
        onViewSession={setViewingSession}
      />
  ```

- [ ] **Vérifier** : `npm run dev -- --port 5174`. L'AuthScreen doit s'afficher à la place de HomeScreen. La console ne doit pas avoir d'erreur.

- [ ] **Commit**

  ```bash
  git add src/App.jsx
  git commit -m "feat: add auth gate in App.jsx, route to AuthScreen when not logged in"
  ```

---

## Task 5 : Modifier api.js — inclure user_id dans saveSessionToSupabase

**Files:**
- Modify: `src/lib/api.js`

- [ ] **Étape 1 : Importer le client supabase-js**

  En haut de `src/lib/api.js`, ajouter :

  ```js
  import { supabase } from './supabase';
  ```

- [ ] **Étape 2 : Réécrire saveSessionToSupabase**

  Remplacer la fonction `saveSessionToSupabase` existante par :

  ```js
  export async function saveSessionToSupabase(session) {
    try {
      const { data: { user } } = await supabase.auth.getUser();
      await fetch(`${SUPABASE_URL}/rest/v1/reboot_sessions`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${SUPABASE_ANON_KEY}`,
          'apikey': SUPABASE_ANON_KEY,
          'Prefer': 'resolution=merge-duplicates,return=minimal',
        },
        body: JSON.stringify({
          session_id: session.session_id,
          date: session.date,
          session_data: session,
          user_id: user?.id ?? null,
          updated_at: new Date().toISOString(),
        }),
      });
    } catch (e) {
      console.warn('Supabase save failed (non-blocking):', e);
    }
  }
  ```

- [ ] **Vérifier** : pas d'erreur au démarrage du serveur.

- [ ] **Commit**

  ```bash
  git add src/lib/api.js
  git commit -m "feat: include user_id in saveSessionToSupabase"
  ```

---

## Task 6 : Modifier HomeScreen — user info + historique audits

**Files:**
- Modify: `src/components/screens/HomeScreen.jsx`

- [ ] **Étape 1 : Mettre à jour la signature du composant**

  Remplacer :
  ```js
  export default function HomeScreen({ onStart, savedSession }) {
  ```
  Par :
  ```js
  export default function HomeScreen({ onStart, savedSession, user, onSignOut, onViewSession }) {
  ```

- [ ] **Étape 2 : Ajouter le fetch des sessions passées**

  Ajouter ces imports en haut du fichier :
  ```js
  import { supabase } from '../../lib/supabase';
  ```

  Dans le corps du composant, après `const [showModal, setShowModal] = useState(false);`, ajouter :

  ```js
  const [pastSessions, setPastSessions] = useState([]);

  useEffect(() => {
    if (!user) return;
    supabase
      .from('reboot_sessions')
      .select('session_id, date, session_data')
      .order('date', { ascending: false })
      .limit(5)
      .then(({ data }) => {
        if (data) setPastSessions(data);
      });
  }, [user]);
  ```

- [ ] **Étape 3 : Ajouter l'info user + déconnexion dans la navbar**

  Dans la navbar, remplacer le bouton "Commencer l'audit" par :

  ```jsx
  <div className="flex items-center gap-3">
    <span className="hidden sm:block text-xs text-[#888] truncate max-w-[160px]">{user?.email}</span>
    <button
      onClick={onSignOut}
      className="text-xs font-semibold px-4 py-2 rounded-full border border-[#e0ddd6] text-[#666] hover:bg-[#f5f0ea] transition-colors"
    >
      Déconnexion
    </button>
    <button onClick={scrollToForm} className="text-sm font-semibold px-5 py-2.5 rounded-full transition-colors" style={{ backgroundColor: '#1a1209', color: '#fff' }}>
      Commencer l'audit
    </button>
  </div>
  ```

- [ ] **Étape 4 : Ajouter la section "Mes audits passés" après le Hero**

  Après la balise fermante `</section>` du bloc HERO (ligne ~269), ajouter :

  ```jsx
  {/* MES AUDITS PASSÉS */}
  {pastSessions.length > 0 && (
    <section className="py-10" style={{ backgroundColor: '#FAF7F2' }}>
      <div className="max-w-6xl mx-auto px-6">
        <p className="text-xs font-semibold uppercase tracking-widest mb-4" style={{ color: '#C96442' }}>Mes audits passés</p>
        <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-3">
          {pastSessions.map((s) => {
            const registres = s.session_data?.registres ?? {};
            const total = Object.values(registres).reduce((acc, r) => acc + (r.score ?? 0), 0);
            const dateFormatted = new Date(s.date).toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' });
            return (
              <div key={s.session_id} className="bg-white rounded-2xl border p-4 flex items-center justify-between gap-4" style={{ borderColor: '#e8e0d8' }}>
                <div>
                  <p className="text-xs text-[#888]">{dateFormatted}</p>
                  <p className="text-lg font-bold text-[#1a1209] mt-0.5">{total.toFixed(0)}<span className="text-xs font-normal text-[#bbb]">/100</span></p>
                </div>
                <button
                  onClick={() => onViewSession(s.session_data)}
                  className="text-xs font-semibold px-4 py-2 rounded-full transition-colors"
                  style={{ backgroundColor: '#1a1209', color: '#fff' }}
                >
                  Consulter
                </button>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  )}
  ```

- [ ] **Étape 5 : Mettre à jour le footer**

  Remplacer dans le footer :
  ```jsx
  <p className="text-xs" style={{ color: 'rgba(255,255,255,0.4)' }}>Propulsé par Claude (Anthropic) · Aucune donnée enregistrée</p>
  ```
  Par :
  ```jsx
  <p className="text-xs" style={{ color: 'rgba(255,255,255,0.4)' }}>Propulsé par Claude (Anthropic) · Données sécurisées par Supabase</p>
  ```

- [ ] **Vérifier** :
  - Se connecter avec magic link ou Google
  - HomeScreen s'affiche avec l'email en haut à droite et le bouton Déconnexion
  - Si des sessions existent pour cet user → section "Mes audits passés" visible
  - Bouton "Consulter" → DiagnosticScreen s'affiche avec les données

- [ ] **Commit**

  ```bash
  git add src/components/screens/HomeScreen.jsx
  git commit -m "feat: add user info, signout, and past sessions history in HomeScreen"
  ```

---

## Task 7 : Ajouter bouton "Retour" dans DiagnosticScreen

**Files:**
- Modify: `src/components/screens/DiagnosticScreen.jsx`

> Quand DiagnosticScreen est affiché en consultation de session passée, il faut pouvoir revenir à HomeScreen.

- [ ] **Étape 1 : Ajouter le prop onBack**

  Modifier la signature :
  ```js
  export default function DiagnosticScreen({ registres, diagnostic, onBack }) {
  ```

- [ ] **Étape 2 : Ajouter le bouton retour sous le bouton export**

  Après le bouton "Exporter en PDF", ajouter :

  ```jsx
  {onBack && (
    <button
      onClick={onBack}
      className="no-print mt-2 w-full border border-[#e0ddd6] text-[#666] py-3 text-sm font-bold tracking-widest uppercase transition-colors hover:bg-[#f5f0ea]"
    >
      ← Retour
    </button>
  )}
  ```

- [ ] **Vérifier** : en cliquant "Consulter" depuis HomeScreen → DiagnosticScreen avec bouton "← Retour" → retour sur HomeScreen.

- [ ] **Commit**

  ```bash
  git add src/components/screens/DiagnosticScreen.jsx
  git commit -m "feat: add optional onBack button in DiagnosticScreen for past session view"
  ```

---

## Vérification finale

- [ ] Ouvrir `http://localhost:5174` → AuthScreen s'affiche
- [ ] Connexion via magic link → email reçu, clic sur lien → connecté sur HomeScreen
- [ ] Connexion via Google → redirect OAuth → connecté sur HomeScreen
- [ ] Lancer un audit complet → session sauvegardée avec `user_id` dans Supabase
- [ ] Retourner sur HomeScreen → section "Mes audits passés" affiche la session
- [ ] Cliquer "Consulter" → DiagnosticScreen avec les données, bouton "← Retour" fonctionnel
- [ ] Cliquer "Déconnexion" → retour sur AuthScreen
- [ ] Déployer sur Vercel : `git push` → vérifier sur https://reboot-v3.vercel.app

---

## Notes

- Les sessions existantes sans `user_id` (NULL) restent dans la table mais ne seront visibles par personne — c'est voulu (pas de migration nécessaire).
- Le mode `?preview=1` reste accessible sans auth (garde déjà en place dans App.jsx).
- Si l'utilisateur complète un audit sans être connecté (impossible avec ce flow, mais en cas de bug) : `user_id` sera `null`, la session sera sauvegardée mais pas visible dans l'historique.
